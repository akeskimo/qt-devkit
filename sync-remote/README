SUMMARY

Tool for transferring source files and git index to remote destination.

INSTALLATION

1. Copy rsync files to home directory

  mkdir $HOME/sync-remote
  cp -r sync-remote/ $HOME

2. Update sync-remote alias with your own configuration.

  nano $HOME/sync.remote.alias

3. Create alias and execute sync.

 . $HOME/sync-remote/sync.remote.alias
 osync-remote

